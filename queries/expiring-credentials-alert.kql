AppRegistrationCredentialExpiry_CL
| summarize arg_max(TimeGenerated, *) by ApplicationId, CredentialKeyId
| where CredentialType != 'None' and DaysToExpiry < 30
| summarize ExpiringCredentials = count()